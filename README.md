# agents-skills

Cursor / Agent skills 备份仓库（`~/.agents/skills`）。

- 数量：303
- 账号：ZeroTokenClaw
- 用途：本地 skill 版本备份与同步

## 使用

将仓库内容同步到 Cursor skills 目录，例如：

```bash
git clone https://github.com/ZeroTokenClaw/agents-skills.git ~/.agents/skills
```

每个子目录为一个 skill（含 `SKILL.md`）。
